import { LiveLobby } from "@/components/live/LiveLobby";

export const metadata = {
  title: "Live Casino | RAPID ROLE",
  description: "Real dealers, real-time gameplay. Join the live floor.",
};

export default function LiveCasinoPage() {
  return <LiveLobby />;
}
